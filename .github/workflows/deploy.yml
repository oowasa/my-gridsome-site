name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    # 下载源码
    - name: Checkout
      uses: actions/checkout@master

    # 打包构建
    - name: Build
      uses: actions/setup-node@master
    - run: npm install
    - run: npm run build

    - name: SSH Remote Commands
        uses: appleboy/ssh-action@master
        with:
          host: "114.55.165.204"
          username: "root"
          password: "Wan3243065yycheng"
    # 用SCP将文件传到远端服务器
    - name: SCP Files to aliyun
      uses: appleboy/scp-action@master
      with:
        host: "114.55.165.204"
        username: "root"
        password: "Wan3243065yycheng"
        source: "./dist"
        target: "/home/my-gridsome-site"